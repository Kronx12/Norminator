#   if     MAX(  a ,   b)    a>   b?a:b
	#include		  <unistd.h>
	#ifdef      MAX(  a ,   b)    a>   b?a:b
			#include "gobal.h"
	#endif
	#            define      MAX(  a ,   b)    a>   b?a:b
#   endif
#  include <stdio.h>
